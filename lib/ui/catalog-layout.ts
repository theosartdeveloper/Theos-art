/** Wide content shell for catalogue / browse pages (more items per scroll). */
export const CATALOG_SHELL = 'catalog-shell'

/** Dense product / course / gallery grids on catalogue pages. */
export const CATALOG_GRID = 'catalog-grid'

/** Slightly larger cards (trainings with richer info). */
export const CATALOG_GRID_COMFORT = 'catalog-grid-comfort'
